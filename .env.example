# GitHub Copilot CLI Authentication
# Fine-grained Personal Access Token with Copilot Requests permission
# Generate at: https://github.com/settings/personal-access-tokens/new
# Required permissions: Copilot Requests, Repository access
GH_TOKEN=github_pat_your_fine_grained_token_here
GITHUB_TOKEN=github_pat_your_fine_grained_token_here

# Node.js Configuration for Copilot CLI (v22+ required)
NODE_OPTIONS=--max-old-space-size=4096

# Copilot CLI Configuration
COPILOT_CONFIG_DIR=~/.copilot

# Project Configuration
PROJECT_NAME=YAML-to-Codebase-Factory
BUILD_OUTPUT_DIR=generated_builds
ARTIFACT_RETENTION_DAYS=30

# GitHub Repository Settings (auto-detected in CI/CD)
# GITHUB_REPOSITORY=owner/repo-name
# GITHUB_REF=refs/heads/main
# GITHUB_SHA=commit-sha

# Optional: Custom Copilot Model Selection
# Available: claude-sonnet-4.5, claude-sonnet-4, gpt-5
# COPILOT_MODEL=claude-sonnet-4.5

# Development vs Production Mode
ENVIRONMENT=production
DEBUG=false

# Trusted Directories for Headless Operation
# Comma-separated list of directories Copilot CLI can operate in
TRUSTED_DIRS=/github/workspace,/Users/runner/work,/tmp/ci-builds

# Artifact Storage
UPLOAD_ARTIFACTS=true
COMPRESS_ARTIFACTS=true

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# Security Settings
# DANGER: Only set ALLOW_ALL_TOOLS=true in trusted CI/CD environments
ALLOW_ALL_TOOLS=true
AUTO_APPROVE_GENERATION=false
REQUIRE_CONFIRMATION=true